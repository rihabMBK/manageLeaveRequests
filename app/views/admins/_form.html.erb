<%= form_with(model: admin, local: true) do |form| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idAdmin %>
    <%= form.number_field :idAdmin %>
  </div>

  <div class="field">
    <%= form.label :passwordAdmin %>
    <%= form.text_field :passwordAdmin %>
  </div>

  <div class="field">
    <%= form.label :emailAdmin %>
    <%= form.text_field :emailAdmin %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
